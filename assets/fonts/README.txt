If text renders as tofu (□), try a font with wider glyph coverage:
- NotoSansThai-Variable.ttf
- NotoSansThaiUI-Regular.ttf
- Sarabun-Regular.ttf

Place the .ttf here and update DEFAULT_FONT in app/pipeline.py or pass a Path to MangaPipeline.
